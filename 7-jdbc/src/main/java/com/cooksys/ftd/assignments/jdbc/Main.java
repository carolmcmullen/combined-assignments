package com.cooksys.ftd.assignments.jdbc;

import java.io.File;

import javax.xml.bind.JAXBContext;
import javax.xml.bind.JAXBException;
import javax.xml.bind.Marshaller;
import javax.xml.bind.Unmarshaller;

import com.cooksys.ftd.assignments.jdbc.model.City;
import com.cooksys.ftd.assignments.jdbc.model.Interest;
import com.cooksys.ftd.assignments.jdbc.model.State;

public class Main {

    public static void main(String[] args) {
     // TODO
 
    	
 }
  
      /*public static void writeConfig(Config config, File studentFilePath, JAXBContext jaxb) {
  	  try {

  		
  		Marshaller jaxbMarshaller = jaxb.createMarshaller();

  		// output pretty printed
  		jaxbMarshaller.setProperty(Marshaller.JAXB_FORMATTED_OUTPUT, true);

  		jaxbMarshaller.marshal(config, studentFilePath);
  		jaxbMarshaller.marshal(config, System.out);

  	      } catch (JAXBException e) {
  		e.printStackTrace();
  	      }
    	
    } */
     

    }


